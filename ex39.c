#include <stdio.h>

int main(){
    char stringa[]= "ciao"; // in questo caso non serve mettere il backslash zero, perchè scritto così fa tutto da solo
    printf("%s \n", stringa);
    return (0);

}
